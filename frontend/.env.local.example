# Copy to .env.local and adjust for your local dev setup.
#
# Backend FastAPI URL. Default ke 127.0.0.1:8000 (uvicorn dev).
# Prod: ganti ke domain backend production (Railway/Azure per ADR-001).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
